# RustCPG-Detect
Compiler Property Graph–based vulnerability detection system for Rust using LLVM IR, GraphCodeBERT embeddings, and GNN models for fine-grained multi-class vulnerability classification.
